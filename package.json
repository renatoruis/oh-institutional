{
  "name": "open-heavens-institucional",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "cp config.js styles.css public/ 2>/dev/null || true",
    "build": "pnpm run prebuild && vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "dompurify": "^3.3.1"
  }
}
